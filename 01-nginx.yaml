- name: nginx installation
  hosts: web
  remote_user: root

  tasks:
  - name: install nginx
    ansible.builtin.yum:
      name: nginx
      state: latest

  - name: start nginx
    ansible.builtin.service:
      name: nginx
      state: started